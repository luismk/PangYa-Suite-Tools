# MySQL migrations

This project contains the MySQL EF Core migrations for `PangyaSchemaDbContext`.
Set `Database:Engine` to `MYSQL` and provide `ConnectionStrings:Pangya` through the
shared appsettings file or environment variables before using the EF CLI.
