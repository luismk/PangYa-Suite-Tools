# PostgreSQL migrations

This project contains the PostgreSQL EF Core migrations for `PangyaSchemaDbContext`.
Set `Database:Engine` to `POSTGRESQL` and provide `ConnectionStrings:Pangya` through
the shared appsettings file or environment variables before using the EF CLI.
